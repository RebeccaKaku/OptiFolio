# TODO — Remaining Issues

**Commit**: `22bc8b0` | **Tests**: 983 passed | **Session**: FinData → packages refactoring + Jules cleanup

---

## P0 — Data Integrity & Verification

### 20. Remove YAML fallback from portfolio loading
**Files**: `src/services/portfolio_service_v2.py`, `src/core/paths.py`, `src/runtime/bootstrap.py`, `src/services/boc_pdf_pipeline.py`

`_load_portfolio()` currently has a SQLite → YAML fallback chain. The YAML path is migration-era crutch. The SQLite book (`local/portfolio_book.sqlite`, 192KB) is the canonical source.

- Delete `_load_holdings_from_yaml()`, `_load_cash_from_yaml()`, `_resolve_config_path()`
- `_load_portfolio()` raises if no confirmed batch exists — no silent degradation
- Remove `get_portfolio_config_path()` from `paths.py`; remove `ensure_local_portfolio()` from `bootstrap.py`
- Remove `portfolio.yaml` loading from `boc_pdf_pipeline.py`

### 21. Remove hardcoded fallback data
**Files**: `src/asset_importer.py`, `src/core/valuation.py`

- `OFFLINE_ASSET_FALLBACKS` (lines 8-14): hardcoded metadata for 6 assets. These exist in `config/asset_registry.yaml`. Delete.
- `DEFAULT_FALLBACK_RATES` (valuation.py:38): hardcoded FX rates. Audit C2 found `5y_cn` off by 2.2pp. Replace with stored-observation lookup or raise `FxRateError`.

### 27. End-to-end verification — "My Money" flow
**Scope**: Full stack integration test

Prove the system works end-to-end:
1. Create account → add product → confirm snapshot batch
2. Read SQLite book → get prices from findata → run valuation
3. Call My Money API → verify response uses real (not random) data
4. Ghostfolio-compatible endpoint returns correct data

---

## P1 — Structure Cleanup

### 19. Remove `_v2` naming suffix
**Files**: `src/services/portfolio_service_v2.py`, `src/api/fastapi_app.py`, `src/services/application.py`, all tests

- `PortfolioServiceV2` → `PortfolioService` (v1 was deleted)
- API routes: `/api/portfolio/v2/` → `/api/portfolio/`
- `EnhancedAssetManager` → `AssetManager` (`AssetManager` was deleted by Jules #180)

### 22. Simplify asset import chain (3 layers → 1)
**Files**: `src/asset_importer.py`, `src/core/enhanced_asset_manager.py`, `src/services/asset_service.py`

Current chain: `AssetImporter` → `EnhancedAssetManager` → `AssetService`. Collapse to direct `findata.fd` calls.

### 23. Wire or remove `IngestionService` stub
**Files**: `src/services/application.py`

`IngestionService` returns `{"records": [], "message": "Ingestion pipeline not yet wired"}`. Either wire to orchestrator or delete.

---

## P2 — Duplicate Modules & Schema

### 24. Consolidate duplicate ValuationEngine and ValuationResult
**Files**: `src/core/valuation.py`, `src/core/book_valuation.py`, `src/domain/models.py`

Two `ValuationEngine` classes + two `ValuationResult` dataclasses. CONTRACTS says "Phase 3 will merge."

### 28. CANONICAL_MARKET_COLUMNS / STORE_VERSION single source
**Files**: `optifolio_contracts/market_data.py`, `findata/store/schemas.py`

Defined in TWO places. Target design (from DECISIONS_PENDING Q2):
- `contracts/market_data.py` owns column names and version (authoritative)
- `findata/store/schemas.py` imports from contracts, adds pandas dtype/normalization helpers
- Delete the duplicate definitions in `findata/store/schemas.py`

### 26. Decide fate of portfolio_history.parquet
**Files**: `src/core/portfolio_history.py`

`PortfolioHistoryTracker` writes to `local/portfolio_history.parquet` — separate from SQLite. Merge or keep.

---

## P3 — Test Hygiene

### 25. Clean up 54 test files
**Scope**: All of `tests/`

- Old identifier formats: bare `510300`, `000198`, `sh600519` → qualified (`fund.cn.etf.sh.510300`, etc.)
- Remove references to deleted modules (`src/data_foundation`, `src/core/symbols`, `FinData`)
- Consolidate `test_ghostfolio_compat.py` + `test_ghostfolio_export.py`
- Consolidate duplicate `TestFdPrices` in `test_findata_storage.py` and `test_findata_serving.py` (audit M4)

---

## Known Bugs — Status

| # | Issue | Status |
|---|-------|--------|
| B1 | `asset_manager.py:359` import from deleted `src.data_core.fetchers.factory` | ✅ Fixed — file deleted by Jules #180 |
| B2 | `valuation.py:177` hardcoded FX fallback used before stored rates | ❌ Still present — covered by TODO #21 |
| B3 | `CANONICAL_MARKET_COLUMNS` in both `contracts/market_data.py` and `findata/store/schemas.py` | ❌ Still present — covered by TODO #28 |
| B4 | `STORE_VERSION` in both `contracts/market_data.py` and `findata/store/schemas.py` | ❌ Still present — covered by TODO #28 |

## Audit Issues — Status

| # | Issue | Status |
|---|-------|--------|
| H1 | `ohlcv()` returns only `adj_close` | ✅ Fixed (pre-refactor) |
| H2 | QualityGate duplicate repository | ❓ Needs verification |
| H3 | `_is_duplicate` never canonicalizes (Check 8) | ❓ Needs verification |
| H4 | `_trigger_refresh` dead stub | ❓ Needs verification |
| H5 | `check_stale_prices` wrong dict level | ✅ Fixed (pre-refactor) |
| H6 | AlertEngine never wired | ✅ Fixed |
| H7 | static_dashboard hardcoded date | ✅ Fixed |
| H8 | dashboard_engine `np.random` dummy data | ❓ Needs verification (dashboard_engine.py deleted?) |
| H9 | Ghostfolio stubs | ✅ Fixed |

## Identifier Convention (finalized)

```
equity.us.<ticker>              equity.us.aapl
equity.cn.<sh|sz|bj>.<code>    equity.cn.sh.600519
fund.cn.<subtype>.<code>        fund.cn.money.000198   (subtype from akshare 基金类型)
fund.cn.etf.<sh|sz>.<code>     fund.cn.etf.sh.510300
wmp.cn.<bank>.<code>            wmp.cn.boc.amhqlxttusd01b
fx.<base>_<quote>.spot          fx.usd_cny.spot
rate.<country>.<index>.<tenor>  rate.cn.shibor.1y
```

Fund subtypes: `money`, `mixed`, `bond`, `stock`, `index`, `qdii`, `fof`. No `open` — use real type from akshare.
