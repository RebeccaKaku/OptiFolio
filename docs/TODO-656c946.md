# TODO — Remaining Issues

**Commit**: `e21ca58` | **Tests**: 837 passed | **Session**: 2026-06-24 cleanup

---

## ✅ Completed This Session

| # | Task | Status |
|---|------|--------|
| 20 | Remove legacy portfolio-file fallback — SQLite only | ✅ Done |
| 21 | Remove hardcoded fallback data (OFFLINE_ASSET_FALLBACKS + DEFAULT_FALLBACK_RATES) | ✅ Done |
| 28 | CANONICAL_MARKET_COLUMNS single source | ✅ Jules #186 |
| 25 | Test cleanup — delete 146 unwired/scaffold tests (983→837, 95s→69s) | ✅ Done |
| B1 | asset_manager.py crash | ✅ Deleted by Jules #180 |
| B3/B4 | CANONICAL_MARKET_COLUMNS/STORE_VERSION dual definition | ✅ Fixed |

---

## P0 — Remaining

### 19. Remove `_v2` naming suffix
**Files**: `src/services/portfolio_service_v2.py`, `src/api/fastapi_app.py`, all tests

- `PortfolioServiceV2` → `PortfolioService`
- API routes: `/api/portfolio/v2/` → `/api/portfolio/`
- `EnhancedAssetManager` → `AssetManager`
- Risk: large rename, ~20 files. Do in a focused session.

### 27. E2E verification — "My Money" flow
Prove: create account → add product → confirm snapshot → valuation → API returns real data.

---

## P1 — Nice to Have

| # | Task | Risk |
|---|------|------|
| 22 | Simplify asset import chain (3 layers → 1) | Medium — changes application.py wiring |
| 23 | IngestionService stub — keep or wire | Low — 6 lines, harmless |
| 24 | Consolidate duplicate ValuationEngine | High — two engines with different semantics |
| 26 | portfolio_history.parquet → SQLite? | Deferred — separate concern |

---

## Decided

- **Identifier format**: `{class}.{scope}.{qualifier}.{local_id}` per `identifiers.py`
- **Fund subtypes**: `money`, `mixed`, `bond`, `stock`, `index`, `qdii`, `fof` — derived from akshare `基金类型`
- **PortfolioHistory**: keep as Parquet for now (append-only valuation log, separate concern from SQLite book)
- **IngestionService stub**: keep (harmless, clear message)
- **No open Jules issues**: all closed or completed
