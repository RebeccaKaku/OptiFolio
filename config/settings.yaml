system:
  mode: "live" # backtest / paper / live

# 注意：资产配置已移动到 config/candidates.yaml
# 如需查看候选资产池，请参考 candidates.yaml 文件

parameters:
  # 注意：实盘模式下，start_date 应该动态取"过去一年"，这里先写死
  start_date: "2023-01-01" 
  end_date: "2024-02-07" # 假设今天是这个日期
  risk_free_rate: 0.04
  risk_aversion: 3.0
  lookback_period: 252